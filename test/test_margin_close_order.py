#
# test/test_margin_close_order.py
#
# Margin Close Order Macro Test
#
# RssMarginCloseOrder_V の直接テスト
#
# 目的:
#   ・楽天RSSの RssMarginCloseOrder_V が正常に呼び出せることを確認する
#   ・RssMarginCloseOrder_V に渡す19引数の個数・順序・値を確認する
#   ・本体の MarginCloseOrder.submit() を介さず、
#     信用返済注文マクロ単体で動作確認する
#
# 実行:
#   python -m test.test_margin_close_order
#
# 今回の注文条件:
#   ・銘柄      : 7203
#   ・信用返済
#   ・売り返済
#   ・1日信用
#   ・通常注文
#   ・成行
#   ・100株
#   ・本日中
#
# 注意:
#   ・本体コードは変更しない
#   ・RssMarginCloseOrder_V は楽天RSS側の機能
#   ・このテストでは RakutenClient.run_macro() を直接呼び出す
#


from market.rakuten.rakuten_client import RakutenClient


def main():

    # debug環境の楽天RSSクライアントを生成
    market = RakutenClient("debug")

    try:

        # Excel接続
        market.open()

        #
        # RssMarginCloseOrder_V テスト
        #
        # RssMarginCloseOrder_V は19個の引数を受け取る。
        #
        # 引数の仕様:
        #
        #  1  発注ID
        #  2  銘柄コード
        #  3  売買区分
        #  4  注文区分
        #  5  SOR区分
        #  6  信用区分
        #  7  注文数量
        #  8  価格区分
        #  9  注文価格
        # 10  執行条件
        # 11  注文期限
        # 12 口座区分
        # 13  建日
        # 14  建単価
        # 15  建市場
        # 16 逆指値条件価格
        # 17 逆指値条件区分
        # 18 逆指値価格区分
        # 19 逆指値価格
        #

        args = (
            105,        #  1 発注ID
            # "9432",     #  2 銘柄コード NTT
            # "7203",     #  2 銘柄コード
            # "6613",     #  2 銘柄コード QDレーザー
            # "3436",     #  2 銘柄コード SUMCO
            "5803",     #  2 銘柄コード フジクラ
                        #   「銘柄コード.市場」の形式で入力。 市場は省略可。
            1,          #  3 売買区分 (1：売り(売理) / 3：買い(買理))
            0,          #  4 注文区分 (0：通常注文 / 1：逆指値付注文 / 2：逆指値待機注文)
            1,          #  5 SOR区分 (0：通常注文 / 1：SOR注文)
            1,          #  6 信用区分 (1：制度（6ヶ月）/2：一般（無期限）/3：一般（14日）/4：一般（1日）)
            100,        #  7 注文数量
            0,          #  8 価格区分 (0：成行 / 1：指値)
            "",         #  9 注文価格 成行の場合は省略
            1,          # 10 執行条件 (1：本日中 / 2：今週中 / 3：寄付 / 4：引け / 5：期間指定 / 6：大引不成立 / 7：不成)
            "",         # 11 注文期限 執行条件が5：期間指定の場合に使用
            0,          # 12 口座区分 (0：特定 / 1：一般 / 2：NISA / 3：旧NISA)
            "20260908", # 13 建日 ★1建玉と完全に一致している必要があり (YYYYMMDD)
            5162.8,     # 14 建単価 ★1建玉と完全に一致している必要があり
            5,          # 15 建市場 ★1建玉と完全に一致している必要があり (1：東証 / 4：JNX / 5：JAX / 6：Chi-X)
            "",         # 16 逆指値条件価格
            "",         # 17 逆指値条件区分 (1：以上 / 2：以下)
            "",         # 18 逆指値価格区分 (0：成行 / 1：指値)
            "",         # 19 逆指値価格
        )

        print("RUN RssMarginCloseOrder_V")

        #
        # 楽天RSSの RssMarginCloseOrder_V を直接実行
        #
        result = market.run_macro("RssMarginCloseOrder_V", *args)

        print(f"RESULT : {result}")
        # RESULT : 注文ID=999 は既に使用済みです。
        # RESULT : 発注ロック中(発注を行うには発注機能を有効にしてください)

    except Exception as e:
        print("ERROR")
        print(type(e).__name__)
        print(e)

    finally:
        # Excel接続を終了
        market.close()

if __name__ == "__main__":
    main()



# 発注ID 関数名                    発注日      発注時刻  注文番号 発注結果
# 104	国内株式 現物注文(VBA)		2026/09/08	01:52:18	-	エラー[売却数量が発注可能数量を超えています。]
# 996	国内株式 信用返済注文(VBA)	2026/09/08	13:15:15	-	エラー[該当する建玉情報がありません。]



# 1. 返済対象（建玉）の「3つのキー」が一致しているか
# 　RssMarginCloseOrder_V 関数で建玉を指定する際、以下の3つの情報が実際の建玉と完全に一
# 　致している必要があります。
#
# ・建日（新規約定日）: 日付の形式（例：2026/09/08 やシリアル値など）が正しく認識さ
# 　れているか確認してください。
# ・建単価（新規約定価格）: 実際の建値と完全に一致している必要があります。
# ・建市場: 「東証」などのコード指定が正しいか確認してください（通常は 1 など数値で指定します）。
# 
# 2. 口座区分・信用区分の不一致
# ・口座区分: 特定口座（2）/ 一般口座（1）/ NISA口座 などが合っているか。
# ・信用区分: 制度信用（1）/ 一般信用（日計り・無期限など）の指定が正しいか。
# 
# 3. 売買区分の指定ミス
# ・買建玉を返済（転売）したい場合は、関数の引数を「売（2）」????にする必要があります。???? 1：売り返済
# ・売建玉を返済（買戻）したい場合は、関数の引数を「買（1）」????にする必要があります。???? 3：買い返済
# ※ここが逆になっていると、存在しない建玉を探しに行ってしまいます。
# 
# 4. すでに他の注文で拘束されていないか
# 　その建玉に対して、すでにマースピII本体やスマホアプリ、あるいは別のExcelセルから
# 　「返済注文」を発注していませんか？
# 
# ・すでに発注中の未約定注文がある場合、その建玉はロックされるため、RSSから重ねて
# 　返済しようとすると「該当する建玉がない」と判定されることがあります。

#💡 おすすめの解決策：建玉一覧関数との連動
# 手入力や固定値で引数を指定しているとエラーが起きやすくなります。
# 楽天RSSには、現在保有している建玉の情報を自動取得する RssMarginPositionList という
# 関数があります。
# 1.別のセル（または別シート）に =RssMarginPositionList() を入力し、現在の正しい 建
#  日・建単価・建市場 などの一覧をExcel上に出力させる。
# 2.RssMarginCloseOrder_V の引数には、その出力されたセルを直接参照（例：C5 など）さ
#  せる。
# 
# この方法をとることで、データの一致ミスをほぼゼロにすることができます。


# 「買建（かいだて）」玉の返済ですね。情報の絞り込みありがとうございます。
# 買建玉を返済（転売）する場合、RssMarginCloseOrder_V の引数設計で特に間違いやすいポ
# イントは以下の2点です。
# ⚠️ 「買建」返済時によくある設定ミス
# 1.売買区分（引数5番目）は「2（売）」になっているか
#  1.買建玉を決済するときは、市場に対して「転売（売り）」を出すことになるため、関
#    数の引数である売買区分には 2（売） を指定する必要があります。ここを「買建だ
#    から 1（買）」にしてしまうとエラーになります。
# 2.建フラグ（引数6番目）は「2（返済）」になっているか
#  1.新規注文ではなく返済注文なので、必ず 2 を指定してください。


# 📋 引数の設定チェックリスト（買建返済用）
# RssMarginCloseOrder_V の引数の並び順と、買建返済時に指定すべき値をまとめました。
# Excelの数式に入力している内容と見比べてみてください。

# 引数の位    置引数名                 買建返済時の指定内容・注意点
# 第1〜4引数  パスワード、銘柄コード、  注文パスワード、銘柄コード、市場コード、返済したい株数を指定。
#            市場、数量
# 第5引数     売買区分                ⚠️ 買建の返済なので 2（売）を指定。
# 第6引数     建フラグ                ⚠️ 返済注文なので 2（返済）を指定。
# 第7引数     執行条件                1（本日中）、2（今週中）など。
# 第8引数     注文価格区分            1（成行）、2（指値）など。
# 第9引数     注文価格成行なら         0、指値なら具体的な価格。
# 第10引数    口座区分                1（一般）、2（特定）など、実際の建玉と一致させる。
# 第11引数    信用区分                1（制度）、2（一般・無期限）、3（一般・日計り）など。
# 第12引数    返済指定方式            ⚠️ 個別指定で返済する場合は 1（個別指定）にする。
# 第13引数    新規約定日              ⚠️ 保有している買建玉の「建日」を正確に指定。
# 第14引数    新規約定価格            ⚠️ 保有している買建玉の「建単価」を正確に指定。
# 第15引数    新規約定市場            ⚠️ 保有している買建玉の「建市場」を正確に指定（通常は 1）。

# ※引数が多いため、カンマ , の数がずれて設定が後ろにズレていないかも合わせてご確認ください。

# 💡 解決のための次のステップ
# もし上記の設定（特に売買区分＝2）が合っているにもかかわらずエラーが出る場合、「新
# 規約定日（建日）」の形式（シリアル値か文字列か）や、「新規約定価格（建値）」のわずか
# なズレが疑われます。
# 
# 前述の通り、RssMarginPositionList 関数を使ってExcel上に一度建玉一覧を吐き出し、そこ
# からセルの参照（例：建日には E5、建値には F5 のようにセルを指定）を行う形に変更
# してみてください。
# 
# 現在、関数の引数は Excelのセルに直接値を手入力 されていますか？それとも 他のセル
# （建玉一覧など）を参照 させていますか？
# もし差し支えなければ、エラーが出ている 実際の数式（ =RssMarginCloseOrder_V(...) の中
# 身） を貼り付けて教えていただければ、どこがズレているか直接お調べできます。